import { createMuiTheme } from "material-ui/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#6ba5f2",
      main: "#3077bf",
      dark: "#004c8e",
      contrastText: "#fff"
    }
  }
});

export default theme;
